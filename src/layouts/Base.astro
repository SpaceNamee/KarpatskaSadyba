---
import '@fontsource/philosopher/700.css';
import '@fontsource-variable/manrope';
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import MobileCta from '../components/MobileCta.astro';

interface Props {
  title: string;
  description: string;
  lang?: 'uk' | 'en';
  /** Картинка для соцмереж (шлях від кореня сайту); за замовчуванням — загальне фото садиби */
  ogImage?: string;
  /** Прибрати сторінку з пошукової видачі (наприклад, 404) */
  noindex?: boolean;
}

const { title, description, lang = 'uk', ogImage = '/og-image.jpg', noindex = false } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site);
const ogImageUrl = new URL(ogImage, Astro.site);
/** GA4 вмикається, коли в змінних середовища збірки задано PUBLIC_GA4_ID (формат G-XXXXXXXXXX) */
const ga4Id = import.meta.env.PUBLIC_GA4_ID as string | undefined;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    {noindex && <meta name="robots" content="noindex, follow" />}
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="theme-color" content="#2f5d3a" />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Карпатська садиба" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:locale" content={lang === 'uk' ? 'uk_UA' : 'en_US'} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="Котедж «Карпатської садиби» серед гір" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />
    <link rel="sitemap" href="/sitemap-index.xml" />
    {ga4Id && (
      <>
        <script async src={`https://www.googletagmanager.com/gtag/js?id=${ga4Id}`}></script>
        <script
          is:inline
          define:vars={{ ga4Id }}
        >
          window.dataLayer = window.dataLayer || [];
          function gtag() { dataLayer.push(arguments); }
          gtag('js', new Date());
          gtag('config', ga4Id);
          document.addEventListener('click', (e) => {
            const tel = e.target instanceof Element && e.target.closest('a[href^="tel:"]');
            if (tel) gtag('event', 'phone_click', { phone: tel.getAttribute('href') });
          });
        </script>
      </>
    )}
  </head>
  <body>
    <a class="skip-link" href="#main">{lang === 'uk' ? 'Перейти до вмісту' : 'Skip to content'}</a>
    <Header lang={lang} />
    <main id="main">
      <slot />
    </main>
    <Footer lang={lang} />
    <MobileCta lang={lang} />
  </body>
</html>
